<Window x:Class="WarehouseMaster.WPF.Windows.AuthWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WarehouseMaster.WPF.Windows"
        mc:Ignorable="d"
        Title="Авторизация" Height="200" Width="350"
        WindowStartupLocation="CenterScreen">
    <Grid Background="#ADBD91">
        <Border BorderThickness="1" Margin="20" CornerRadius="20">
            <Border.Effect>
                <DropShadowEffect BlurRadius="10"/>
            </Border.Effect>
            
            <Grid Background="#ADBD91">

                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="0.3*"/>
                    <ColumnDefinition/>
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition/>
                    <RowDefinition Height="auto"/>
                </Grid.RowDefinitions>

                <StackPanel Grid.Column="0" HorizontalAlignment="Right" VerticalAlignment="Bottom" Background="#CCDEAA" Height="56">

                    <TextBlock Text="Логин:"/>
                    <TextBlock Text="Пароль:"/>

                </StackPanel>

                <StackPanel Grid.Column="1" HorizontalAlignment="Left" VerticalAlignment="Bottom" Background="#CCDEAA">

                    <TextBox x:Name="LoginTextBlock" Width="200"/>
                    <TextBox x:Name="PassworkTextBlock" Width="200"/>

                </StackPanel>

                <Button Click="Auth_Click" Content="Войти" Width="150" Margin="10"
                    Grid.Row="1" Grid.Column="1" HorizontalAlignment="Left" VerticalAlignment="Top"/>
            </Grid>
        </Border>
    </Grid>
</Window>
